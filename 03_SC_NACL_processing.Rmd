---
title: "03_SC_NACL_processing"
output: html_document
date: "2025-05-28"
---


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

Load the libraries
```{r echo=FALSE}
library(Seurat)
library(dplyr)
library(Matrix)
library(RColorBrewer)
library(pdist)
library(plyr)
library(reshape)
library(ggplot2)
library(reshape2)
library(openxlsx)
library(gplots)
library(corrplot)
library(viridis)
library(reshape2)
library(gridExtra)
library(cowplot)
library(reticulate)
library(umap)
library(monocle3)
library(SeuratWrappers)
library(patchwork)
library(viridis)
library(tidyverse)
library(smplot2)
library(dittoSeq) 
```

```{r}
mouse_seurat_filtered_NACL <- readRDS('04_NACL/04_NACL_sc_mouse_filtered_merged_seurat.rds')
```

```{r}
VlnPlot(mouse_seurat_filtered_NACL, features = c("nFeature_RNA", "nCount_RNA", "percent.mt"), ncol = 3)
```

```{r}
plot1 <- FeatureScatter(mouse_seurat_filtered_NACL, feature1 = "nCount_RNA", feature2 = "percent.mt")
plot2 <- FeatureScatter(mouse_seurat_filtered_NACL, feature1 = "nCount_RNA", feature2 = "nFeature_RNA")
plot1 + plot2
```

```{r}
# 1. Store current identity in a new metadata column
mouse_seurat_filtered_NACL$original_ident <- Idents(mouse_seurat_filtered_NACL)

# Unified identity
Idents(mouse_seurat_filtered_NACL) <- "NACL"
```

# Normalize and find the variable features

```{r}
mouse_seurat_filtered_NACL <- NormalizeData(mouse_seurat_filtered_NACL, normalization.method = "LogNormalize", scale.factor = 10000)

mouse_seurat_filtered_NACL <- FindVariableFeatures(mouse_seurat_filtered_NACL, selection.method = "vst", nfeatures = 2000)
```

```{r}
# Identify the 10 most highly variable genes
top10 <- head(VariableFeatures(mouse_seurat_filtered_NACL), 10)
```


```{r}
# plot variable features with and without labels
plot1 <- VariableFeaturePlot(mouse_seurat_filtered_NACL)
plot2 <- LabelPoints(plot = plot1, points = top10, repel = TRUE)
plot1 + plot2
```

```{r}
all.genes <- rownames(mouse_seurat_filtered_NACL)
mouse_seurat_filtered_NACL <- ScaleData(mouse_seurat_filtered_NACL, features = all.genes)
```

```{r}
mouse_seurat_filtered_NACL <- RunPCA(mouse_seurat_filtered_NACL, features = VariableFeatures(object = mouse_seurat_filtered_NACL))
```

```{r}
VizDimLoadings(mouse_seurat_filtered_NACL, dims = 1:2, reduction = "pca")
```

```{r}
DimHeatmap(mouse_seurat_filtered_NACL, dims = 1:15, cells = 500, balanced = TRUE)
```


```{r}
ElbowPlot(mouse_seurat_filtered_NACL, ndims = 30)
```

```{r}
mouse_seurat_filtered_NACL <- FindNeighbors(mouse_seurat_filtered_NACL, dims = 1:20)
mouse_seurat_filtered_NACL <- FindClusters(mouse_seurat_filtered_NACL)
```

```{r}
mouse_seurat_filtered_NACL <- RunUMAP(mouse_seurat_filtered_NACL, dims = 1:20)
```

```{r}
DimPlot(mouse_seurat_filtered_NACL, reduction = "pca")
```

```{r}
DimPlot(mouse_seurat_filtered_NACL, reduction = "umap", group.by = "original_ident")
```

```{r}
DimPlot(mouse_seurat_filtered_NACL, reduction = "umap", group.by = "group")
```

```{r}
mouse_seurat_filtered_NACL[["joined"]] <- JoinLayers(mouse_seurat_filtered_NACL[["RNA"]])
```

```{r}
s.genes <- cc.genes$s.genes
g2m.genes <- cc.genes$g2m.genes
```


```{r}
mouse_seurat_filtered_NACL <- CellCycleScoring(mouse_seurat_filtered_NACL, s.features = s.genes, g2m.features = g2m.genes, set.ident = TRUE, assay="joined")
```


```{r}
DimPlot(mouse_seurat_filtered_NACL, reduction = "umap", group.by = "Phase")
```

Write metadata after calculating the cell cycle score
```{r}
write.table(mouse_seurat_filtered_NACL@meta.data, file = "04_NACL/cell_cycle_scored_metadata.tsv",
            sep = "\t", quote = F)
```


Remove joined layer
```{r}
mouse_seurat_filtered_NACL[["joined"]] <- NULL
```


Regressing the cell cycle score
```{r}
mouse_seurat_filtered_NACL <- ScaleData(mouse_seurat_filtered_NACL, vars.to.regress = c("S.Score", "G2M.Score"), features = rownames(mouse_seurat_filtered_NACL))
```

```{r}
saveRDS(mouse_seurat_filtered_NACL, 
        file = "04_NACL//04_NACL_sc_mouse_filtered_merged_seurat_cc.rds")
```


```{r}
mouse_seurat_filtered_NACL <- RunPCA(mouse_seurat_filtered_NACL, 
                                    features = VariableFeatures(mouse_seurat_filtered_NACL), 
                                    nfeatures.print = 10)
```


```{r}
mouse_seurat_filtered_NACL <- FindNeighbors(mouse_seurat_filtered_NACL, dims = 1:20)
mouse_seurat_filtered_NACL <- FindClusters(mouse_seurat_filtered_NACL, resolution = 0.5)
mouse_seurat_filtered_NACL <- RunUMAP(object = mouse_seurat_filtered_NACL, dims = 1:20)
```

```{r}
DimPlot(mouse_seurat_filtered_NACL, group.by="seurat_clusters")
```


```{r}
DimPlot(mouse_seurat_filtered_NACL, group.by="Phase")
```

```{r}
DimPlot(mouse_seurat_filtered_NACL, group.by="group")
```

```{r}
dittoBarPlot(object = mouse_seurat_filtered_NACL, 
             group.by = "seurat_clusters",
             var  = "Phase", scale="percent")
```


```{r}
saveRDS(mouse_seurat_filtered_NACL, 
        file = "04_NACL//04_NACL_sc_mouse_filtered_merged_seurat_cc.rds")
```